module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx,html}"],
  // You can optionally define themes here
  // daisyUI: { themes: ["light", "dark"] },
  plugins: [], // No need to `require('daisyui')` when using @plugin
}
